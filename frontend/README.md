# Frontend - Employee Attendance & Tardiness Management System

Next.js (App Router) + TypeScript + Tailwind CSS.

## Setup

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

Visit http://localhost:3000 — make sure the backend is running on port 8000 first.

Default login: admin@company.com / Admin123!
